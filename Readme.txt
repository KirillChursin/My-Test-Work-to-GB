1. InputValue:
Create method that takes an input value, that determines the length of the first array.

2. ArrayElementsValue:
Creating a method that accepts the values ​​of array elements entered from the keyboard.

3. Counter:
Create the first method, that will count the number of values, ​​that match the task condition.
This is the length of the second array.

4. ValueArray:
Next method, add value, that match the task condition, to second array from the first array.

5. PrintArray:
This method print values from second array to the console.

6. Assigning a variable the length of the first array

7. Create first array.

8. Call method ArrayElementsValue.

9. Create arrayTaskCondidion (with length == Count).

10. Call method ValueArray.

11. Call method PrintArray.
